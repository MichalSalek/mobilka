// import { useColorScheme }   from 'react-native'
import { ColorSchemeKeyof } from '../Colors'




export const useColorSchemeHook = (): ColorSchemeKeyof | undefined | null => {
  return 'light'//useColorScheme()
}
